# vLLM is already installed in the base image (vllm/vllm-openai:nightly)
runpod>=1.7.0
aiohttp>=3.9.0
huggingface_hub>=0.24.0
pyyaml>=6.0
boto3>=1.34.0       # S3-compatible access to RunPod network volumes
